<% if @practices.any? %>
  $('.empty').remove();
  $('.practices_list').append("<%= escape_javascript(raw render(@practices.last))%>");
  $('.inputs input, input[id*=discipline_id], select').val('');
  numeration();
<% end %>
