<%= semantic_form_for @practice, :url => diploma_practice_path(@practice.diploma, @practice), :remote => true, :html => {:'data-type' => 'html'} do |form| %>
  <%= form.error_messages %>
  <%= render :partial => 'practices/form', :locals => {:form => form} %>
  <%= form.buttons do %>
    <%= form.commit_button :button_html => { :class => "button" } %>
    <%= link_to t(:cancel), smart_resource_url,  :class => 'button cancel red', :remote => true, :'data-type' => 'html' %>
  <% end %>
<% end %>

