<%= semantic_form_for @practice, :url => resource_url, :remote => true, :html => {:'data-type' => 'html'} do |form| %>
  <%= render :partial => 'practices/form', :locals => {:form => form, :cancel => true} %>
<% end %>

