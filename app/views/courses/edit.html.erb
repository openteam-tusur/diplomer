<%= semantic_form_for @course, :url => resource_url, :remote => true, :html => {:'data-type' => 'html'} do |form| %>
  <%= render :partial => 'courses/form', :locals => {:form => form, :cancel => true} %>
<% end %>

