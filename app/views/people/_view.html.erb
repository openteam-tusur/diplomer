<%= show_for @person do |p| %>
  <%= p.attribute :surname %>
  <%= p.attribute :name %>
  <%= p.attribute :patrynomic %>
  <%= p.association :user, :label => 'Email' , :using => :email %>
  <%= p.association :roles, :label => 'Роль' %>

<% end %>

