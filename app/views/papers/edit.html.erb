<%= semantic_form_for @paper, :url => resource_url, :remote => true, :html => {:'data-type' => 'html'} do |form| %>
  <%= render :partial => 'papers/form', :locals => {:form => form, :cancel => true } %>
<% end %>

