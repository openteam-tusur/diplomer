<% if @papers.any? %>
  $('.empty').remove();
  $('.papers_list').append("<%= escape_javascript(raw render(@papers.last))%>");
  $('.inputs input, input[id*=discipline_id], select').val('');
  numeration();
<% end %>
