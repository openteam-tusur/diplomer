<%= semantic_form_for @final_state_examination, :url => resource_url, :remote => true, :html => {:'data-type' => 'html'} do |form| %>
  <%= render :partial => 'final_state_examinations/form', :locals => {:form => form, :cancel => true} %>
<% end %>

